[project]
name="hermes-trading"
version="0.1.0"
requires-python=">=3.11"
dependencies=["ccxt","yfinance","pyyaml","httpx","aiofiles","numpy","pandas","rich"]
[build-system]
requires=["hatchling"]
build-backend="hatchling.build"
[tool.hatch.build.targets.wheel]
packages=["hermes_trading"]
